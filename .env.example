# Example environment variables for MedHaven (do NOT commit your real keys)
# Copy this file to .env.local and fill in the real values locally.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional (server-only) service role key - store in CI or secrets manager, never in .env.local that is committed
# SUPABASE_SERVICE_ROLE_KEY=
